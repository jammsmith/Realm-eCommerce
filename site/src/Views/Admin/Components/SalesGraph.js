import React from 'react';
import PropTypes from 'prop-types';

// Styled components
import { SalesGraphWrapper } from '../styledComponents.js';

const SalesGraph = () => {
  return (
    <SalesGraphWrapper>Sales Graph</SalesGraphWrapper>
  );
};

SalesGraph.propTypes = {

};

export default SalesGraph;
