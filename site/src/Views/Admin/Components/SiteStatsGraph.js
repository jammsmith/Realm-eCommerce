import React from 'react';
import PropTypes from 'prop-types';

// Styled components
import { SiteStatsWrapper } from '../styledComponents.js';

const SiteStatsGraph = () => {
  return (
    <SiteStatsWrapper>Site Stats Graph</SiteStatsWrapper>
  );
};

SiteStatsGraph.propTypes = {

};

export default SiteStatsGraph;
