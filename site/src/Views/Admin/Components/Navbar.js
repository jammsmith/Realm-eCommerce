import React from 'react';
import PropTypes from 'prop-types';

// Styled components
import { NavbarWrapper } from '../styledComponents.js';

const Navbar = () => {
  return (
    <NavbarWrapper>Navbar</NavbarWrapper>
  );
};

Navbar.propTypes = {

};

export default Navbar;
